name: 🔗 Extended Mac Session (Auto-Chain)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Duration per chain (hours)'
        required: true
        default: '6'
        type: choice
        options:
          - '4'
          - '5'
          - '6'
      macos_version:
        description: 'macOS version'
        required: true
        default: '14'
        type: choice
        options:
          - '14'
          - '15'
          - '26'
      max_chains:
        description: 'Maximum number of chains'
        required: true
        default: '2'
        type: choice
        options:
          - '1'
          - '2'
          - '3'
      runner_size:
        description: 'Runner size'
        required: true
        default: 'standard'
        type: choice
        options:
          - 'standard'
          - 'large'
          - 'xlarge'
      unique_ip:
        description: 'Ensure unique IP via Cloudflare WARP VPN (ignored when OpenWrt exit is on)'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      openwrt_exit:
        description: 'Boot OpenWrt inside this Mac and send Safari/Chrome through it (Azure IP, no WARP)'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      current_chain:
        description: 'Current chain number (internal use)'
        required: false
        default: '1'

env:
  SESSION_DURATION: ${{ github.event.inputs.duration }}
  MACOS_VERSION: ${{ github.event.inputs.macos_version }}
  MAX_CHAINS: ${{ github.event.inputs.max_chains }}
  CURRENT_CHAIN: ${{ github.event.inputs.current_chain }}

jobs:
  extended-session:
    name: 🔗 Extended Session (macOS ${{ github.event.inputs.macos_version }}, Chain ${{ github.event.inputs.current_chain }}/${{ github.event.inputs.max_chains }})
    runs-on: ${{ github.event.inputs.runner_size == 'xlarge' && format('macos-{0}-xlarge', github.event.inputs.macos_version) || github.event.inputs.runner_size == 'large' && format('macos-{0}-large', github.event.inputs.macos_version) || format('macos-{0}', github.event.inputs.macos_version) }}
    timeout-minutes: 375
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📦 Restore IP History Cache
        uses: actions/cache@v4
        with:
          path: .ip-history.json
          key: ip-history-${{ github.repository_owner }}
          restore-keys: |
            ip-history-

      - name: 🌐 Detect and Track IP
        run: |
          chmod +x scripts/ip-manager.sh
          ./scripts/ip-manager.sh

      - name: 🔒 Setup Unique IP (Cloudflare WARP)
        if: github.event.inputs.unique_ip == 'true' && github.event.inputs.openwrt_exit != 'true'
        run: |
          chmod +x scripts/setup-warp.sh
          ./scripts/setup-warp.sh

      - name: 🔐 Create Admin User for GUI Authentication
        run: |
          # Create a new admin user that will appear in Users & Groups
          ADMIN_USER="yourname"
          echo "MAC_USER=${ADMIN_USER}" >> $GITHUB_ENV
          
          # Generate a secure password
          MAC_PASSWORD=$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | head -c 12)
          
          # Mask password in logs
          echo "::add-mask::${MAC_PASSWORD}"
          echo "MAC_PASSWORD=${MAC_PASSWORD}" >> $GITHUB_ENV
          
          echo "👤 Creating admin user: ${ADMIN_USER}"
          
          # Check if user already exists
          if id "${ADMIN_USER}" &>/dev/null; then
            echo "User ${ADMIN_USER} already exists, setting password..."
            sudo dscl . -passwd /Users/${ADMIN_USER} "" "${MAC_PASSWORD}" 2>/dev/null || \
            sudo sysadminctl -resetPasswordFor "${ADMIN_USER}" -newPassword "${MAC_PASSWORD}" 2>/dev/null || true
          else
            # Find a free UID (starting from 501 for regular users)
            LAST_UID=$(dscl . -list /Users UniqueID | awk '{print $2}' | sort -n | tail -1)
            NEW_UID=$((LAST_UID + 1))
            
            echo "   Creating user with UID: ${NEW_UID}"
            
            # Create the user
            sudo dscl . -create /Users/${ADMIN_USER}
            sudo dscl . -create /Users/${ADMIN_USER} UserShell /bin/zsh
            sudo dscl . -create /Users/${ADMIN_USER} RealName "Your Name"
            sudo dscl . -create /Users/${ADMIN_USER} UniqueID ${NEW_UID}
            sudo dscl . -create /Users/${ADMIN_USER} PrimaryGroupID 20
            sudo dscl . -create /Users/${ADMIN_USER} NFSHomeDirectory /Users/${ADMIN_USER}
            
            # Set password
            sudo dscl . -passwd /Users/${ADMIN_USER} "${MAC_PASSWORD}"
            
            # Add to admin group
            sudo dseditgroup -o edit -a ${ADMIN_USER} -t user admin
            
            # Create home directory
            sudo createhomedir -c -u ${ADMIN_USER} 2>/dev/null || sudo mkdir -p /Users/${ADMIN_USER}
            sudo chown -R ${ADMIN_USER}:staff /Users/${ADMIN_USER} 2>/dev/null || true
          fi
          
          # Verify user was created and is admin
          if id "${ADMIN_USER}" &>/dev/null; then
            echo "✅ User ${ADMIN_USER} created successfully"
            if dseditgroup -o checkmember -m "${ADMIN_USER}" admin 2>/dev/null; then
              echo "✅ User ${ADMIN_USER} is an admin"
            fi
          else
            echo "❌ Failed to create user"
          fi
          
          echo ""
          echo "   Use these credentials for System Settings:"
          echo "   User: ${ADMIN_USER}"
          echo "   Password: (in artifact file)"

      - name: 📊 System Information
        run: |
          chmod +x scripts/system-info.sh
          ./scripts/system-info.sh
          
          echo ""
          echo "🔗 Chain Information:"
          echo "   ├─ Current Chain: ${{ env.CURRENT_CHAIN }} / ${{ env.MAX_CHAINS }}"
          echo "   ├─ Duration: ${{ env.SESSION_DURATION }} hours"
          TOTAL_TIME=$(( ${{ env.CURRENT_CHAIN }} * ${{ env.SESSION_DURATION }} ))
          echo "   └─ Total Time (so far): ${TOTAL_TIME} hours"
          echo ""

      - name: 🎮 Install Parsec (for optional manual use)
        run: |
          chmod +x scripts/install-parsec.sh
          ./scripts/install-parsec.sh

      - name: 🦀 Setup RustDesk
        run: |
          chmod +x scripts/setup-rustdesk.sh
          
          # Generate secure password
          RUSTDESK_PASSWORD=$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | head -c 12)
          
          # Mask password in GitHub Actions logs
          echo "::add-mask::${RUSTDESK_PASSWORD}"
          
          # Save as environment variable (masked)
          echo "RUSTDESK_PASSWORD=${RUSTDESK_PASSWORD}" >> $GITHUB_ENV
          
          export RUSTDESK_PASSWORD
          ./scripts/setup-rustdesk.sh
          
          # Wait for RustDesk to register
          sleep 10

      - name: 🔑 Get RustDesk ID
        run: |
          sleep 5
          
          RUSTDESK_CLI="/Applications/RustDesk.app/Contents/MacOS/rustdesk"
          
          if [ -f "$RUSTDESK_CLI" ]; then
            RUSTDESK_ID=$("$RUSTDESK_CLI" --get-id 2>/dev/null || echo "")
            
            if [ -z "$RUSTDESK_ID" ]; then
              CONFIG_DIR="$HOME/Library/Application Support/RustDesk"
              RUSTDESK_ID=$(cat "${CONFIG_DIR}/id" 2>/dev/null || echo "")
            fi
            
            if [ -n "$RUSTDESK_ID" ]; then
              echo "RUSTDESK_ID=${RUSTDESK_ID}" >> $GITHUB_ENV
              echo "✅ RustDesk ID: ${RUSTDESK_ID}"
            else
              echo "RUSTDESK_ID=Check_Logs" >> $GITHUB_ENV
            fi
          fi

      - name: 🔓 Allow this session IP on the GCP proxy
        if: github.event.inputs.openwrt_exit == 'true'
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          chmod +x scripts/allow-gcp-firewall.sh
          ./scripts/allow-gcp-firewall.sh

      - name: 🧭 Boot OpenWrt exit (Safari/Chrome via localhost proxy)
        if: github.event.inputs.openwrt_exit == 'true'
        env:
          GCP_PROXY_HOST: ${{ secrets.GCP_PROXY_HOST }}
          GCP_PROXY_PORT: ${{ secrets.GCP_PROXY_PORT }}
          GCP_PROXY_USER: ${{ secrets.GCP_PROXY_USER }}
          GCP_PROXY_PASS: ${{ secrets.GCP_PROXY_PASS }}
          WG_OPENWRT_PRIVATE_KEY: ${{ secrets.WG_OPENWRT_PRIVATE_KEY }}
          WG_SERVER_PUBLIC_KEY: ${{ secrets.WG_SERVER_PUBLIC_KEY }}
        run: |
          chmod +x scripts/setup-openwrt.sh
          ./scripts/setup-openwrt.sh

      - name: 💾 Save Credentials (Private File)
        run: |
          mkdir -p credentials
          
          # Create file with credentials (NOT displayed in logs)
          cat > credentials/connection-info.txt << EOF
          ════════════════════════════════════════════════════════════
            EXTENDED SESSION - macOS ${{ env.MACOS_VERSION }} - Chain ${{ env.CURRENT_CHAIN }}/${{ env.MAX_CHAINS }}
          ════════════════════════════════════════════════════════════
          
          Started by: ${{ github.actor }}
          Date/Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
          Run ID: ${{ github.run_id }}
          
          ────────────────────────────────────────────────────────────
          🌐 Network Information:
          ────────────────────────────────────────────────────────────
          
            Public IP:  ${{ env.CURRENT_IP }}
            Location:   ${{ env.IP_CITY }}, ${{ env.IP_COUNTRY }}
            Provider:   ${{ env.IP_ORG }}
            WARP VPN:   ${{ env.WARP_ENABLED || 'false' }}
            OpenWrt:    ${{ env.OPENWRT_ENABLED || 'false' }}
            OpenWrt IP: ${{ env.OPENWRT_EGRESS_IP || 'n/a' }}
            HTTP proxy: ${{ env.OPENWRT_PROXY || 'n/a' }}
            SOCKS5:     ${{ env.OPENWRT_SOCKS || 'n/a' }}
          
          BlackBird (paste this, type HTTP, no user/pass):
            http://127.0.0.1:13128
          
          ────────────────────────────────────────────────────────────
          🍎 macOS Credentials:
          ────────────────────────────────────────────────────────────
          
            User:     ${{ env.MAC_USER }}
            Password: ${{ env.MAC_PASSWORD }}
          
            Use this password for:
            • System Settings authentication
            • Privacy & Security permissions (Input Monitoring, etc.)
            • Installing apps that require admin
            • Parsec permissions setup
          
          ────────────────────────────────────────────────────────────
          🦀 RustDesk Credentials:
          ────────────────────────────────────────────────────────────
          
            ID:       ${{ env.RUSTDESK_ID }}
            Password: ${{ env.RUSTDESK_PASSWORD }}
          
          ────────────────────────────────────────────────────────────
          📖 How to Connect:
          ────────────────────────────────────────────────────────────
          
          1. Download RustDesk: https://rustdesk.com/download
          2. Enter the ID and password above
          3. Click Connect!
          
          ⚠️  New chain = new credentials!
          
          ────────────────────────────────────────────────────────────
          🎮 Optional: Parsec is also installed!
          ────────────────────────────────────────────────────────────
          
          If you prefer Parsec for lower latency:
          1. Connect via RustDesk first
          2. Open Parsec from Applications
          3. Log in with your Parsec account
          4. When prompted for permissions, use macOS password above
          5. Enable hosting and connect from your other device
          
          ════════════════════════════════════════════════════════════
          ⚠️  WARNING: This file contains sensitive credentials.
              Do not share with others.
          ════════════════════════════════════════════════════════════
          EOF

      - name: 📦 Upload Credentials Artifact
        uses: actions/upload-artifact@v4
        with:
          # Artifact name includes user and chain for identification
          name: credentials-${{ github.actor }}-chain${{ env.CURRENT_CHAIN }}-${{ github.run_id }}
          path: credentials/
          retention-days: 1

      - name: 📋 Connection Instructions
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════════════╗"
          echo "║   🔗 EXTENDED SESSION - macOS ${{ env.MACOS_VERSION }} - Chain ${{ env.CURRENT_CHAIN }}/${{ env.MAX_CHAINS }}               ║"
          echo "╚══════════════════════════════════════════════════════════════════╝"
          echo ""
          echo "👤 Session started by: ${{ github.actor }}"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🌐 Public IP: ${{ env.CURRENT_IP }}"
          echo "📍 Location: ${{ env.IP_CITY }}, ${{ env.IP_COUNTRY }}"
          if [ "${{ env.WARP_ENABLED }}" = "true" ]; then
            echo "🔒 VPN: Cloudflare WARP (Unique IP Enabled)"
          fi
          if [ "${{ env.IP_IS_DUPLICATE }}" = "true" ]; then
            echo "⚠️  WARNING: This IP was previously used!"
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "🍎 macOS User: ${{ env.MAC_USER }}"
          echo "🔐 macOS Password: ████████████ (in artifact)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "🔑 RustDesk ID: ${{ env.RUSTDESK_ID }}"
          echo "🔐 RustDesk Password: ████████████ (in artifact)"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📥 HOW TO GET ALL PASSWORDS:"
          echo ""
          echo "   1. Go to the 'Summary' tab of this workflow"
          echo "   2. Download artifact: credentials-${{ github.actor }}-chain${{ env.CURRENT_CHAIN }}-${{ github.run_id }}"
          echo "   3. Open the connection-info.txt file"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "🎮 Parsec is also installed! Use macOS password for permissions."
          echo ""
          echo "🔒 SECURITY: Passwords are NOT displayed in logs."
          echo ""

      - name: ⏱️ Keep Session Alive (with chain trigger)
        run: |
          chmod +x scripts/keep-alive.sh
          
          DURATION_HOURS=${{ env.SESSION_DURATION }}
          KEEPALIVE_INTERVAL=300
          
          # Calculate times
          DURATION_SECONDS=$((DURATION_HOURS * 3600))
          CHAIN_TRIGGER_TIME=$((DURATION_SECONDS - 120)) # 2 minutes before end
          START_TIME=$(date +%s)
          END_TIME=$((START_TIME + DURATION_SECONDS))
          
          echo "⏱️  Keep-alive started"
          echo "   Duration: ${DURATION_HOURS} hour(s)"
          echo "   Next chain trigger: 2 minutes before end"
          echo ""
          
          triggered_chain=false
          
          while true; do
            CURRENT_TIME=$(date +%s)
            ELAPSED=$((CURRENT_TIME - START_TIME))
            REMAINING=$((END_TIME - CURRENT_TIME))
            
            if [ $REMAINING -le 0 ]; then
              echo ""
              echo "⏰ Session time expired."
              break
            fi
            
            # Trigger next chain 2 minutes before end
            if [ $ELAPSED -ge $CHAIN_TRIGGER_TIME ] && [ "$triggered_chain" = false ]; then
              CURRENT_CHAIN=${{ env.CURRENT_CHAIN }}
              MAX_CHAINS=${{ env.MAX_CHAINS }}
              NEXT_CHAIN=$((CURRENT_CHAIN + 1))
              
              if [ $NEXT_CHAIN -le $MAX_CHAINS ]; then
                echo ""
                echo "🔗 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                echo "🔗 Starting next chain (${NEXT_CHAIN}/${MAX_CHAINS})..."
                echo "🔗 New session with new credentials coming soon."
                echo "🔗 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                echo ""
                
                # Trigger next workflow
                gh workflow run extended-session.yml \
                  -f duration=${{ env.SESSION_DURATION }} \
                  -f macos_version=${{ github.event.inputs.macos_version }} \
                  -f max_chains=${{ env.MAX_CHAINS }} \
                  -f runner_size=${{ github.event.inputs.runner_size }} \
                  -f unique_ip=${{ github.event.inputs.unique_ip }} \
                  -f openwrt_exit=${{ github.event.inputs.openwrt_exit }} \
                  -f current_chain=${NEXT_CHAIN}
                
                triggered_chain=true
              else
                echo ""
                echo "⚠️  Maximum chains reached. No more extensions."
              fi
            fi
            
            # Warnings
            if [ $REMAINING -le 900 ] && [ $REMAINING -gt 890 ]; then
              echo ""
              echo "⚠️  15 minutes remaining in this chain!"
            fi
            
            if [ $REMAINING -le 300 ] && [ $REMAINING -gt 290 ]; then
              echo ""
              echo "🚨 5 minutes remaining! Check for new chain credentials."
            fi
            
            # Heartbeat
            HOURS=$((REMAINING / 3600))
            MINUTES=$(((REMAINING % 3600) / 60))
            printf "\r💓 [%02d:%02d remaining] Chain ${{ env.CURRENT_CHAIN }} active - $(date '+%H:%M:%S')     " $HOURS $MINUTES
            
            SLEEP_TIME=$((REMAINING < KEEPALIVE_INTERVAL ? REMAINING : KEEPALIVE_INTERVAL))
            sleep $SLEEP_TIME
          done
          
          echo ""
          echo "👋 Chain ${{ env.CURRENT_CHAIN }} ended."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 💾 Save IP History Cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .ip-history.json
          key: ip-history-${{ github.repository_owner }}-${{ github.run_id }}
